{
  "compute": {
    "eps": { "numeric": 1e-12 },
    "public": { "forces": { "anchor.spring": { "enable": true, "k": 1.0 } } },
    "passes": {
      "capture": { "every": 1, "final_always": true },
      "grad_clip": { "max_norm": null, "max_abs": null }
    }
  }
}
